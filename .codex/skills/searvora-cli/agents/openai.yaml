interface:
  display_name: "Searvora CLI"
  short_description: "Use @searvora/cli to authenticate and run Searvora SEO services, Spider crawls, Analysis, tools, pricing, and audit workflows."
  default_prompt: "Use the Searvora CLI skill to operate @searvora/cli safely. Install or verify the package, authenticate with password-stdin/env, use --profile public and --json, run the correct command group, save raw artifacts, and produce evidence-backed SEO diagnostics without exposing secrets."
